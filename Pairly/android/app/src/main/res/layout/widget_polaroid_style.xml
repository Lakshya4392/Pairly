<?xml version="1.0" encoding="utf-8"?>
<!-- Widget Style 3: Polaroid Style -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@android:color/transparent">

    <!-- Polaroid Frame -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/widget_background_polaroid"
        android:padding="12dp">

        <!-- Photo Area -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#F5F5F5">

            <!-- Photo -->
            <ImageView
                android:id="@+id/widget_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Partner photo"
                android:visibility="gone"/>

            <!-- Empty Polaroid -->
            <LinearLayout
                android:id="@+id/widget_placeholder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center"
                android:background="#FAFAFA">
                
                <ImageView
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:src="@drawable/ic_polaroid_camera"
                    android:tint="#CCCCCC"/>
                
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Capture a moment"
                    android:textSize="13sp"
                    android:textColor="#999999"
                    android:layout_marginTop="12dp"
                    android:fontFamily="cursive"/>
            </LinearLayout>
        </FrameLayout>

        <!-- Polaroid Caption Area -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="12dp"
            android:paddingBottom="8dp">
            
            <TextView
                android:id="@+id/widget_partner_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Partner ❤️"
                android:textSize="14sp"
                android:textColor="#333333"
                android:fontFamily="cursive"
                android:layout_gravity="center"/>
            
            <TextView
                android:id="@+id/widget_timestamp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Just now"
                android:textSize="11sp"
                android:textColor="#999999"
                android:layout_gravity="center"
                android:layout_marginTop="4dp"/>
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>
