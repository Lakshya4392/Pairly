<?xml version="1.0" encoding="utf-8"?>
<!-- Pairly Polaroid Widget - Premium Clean Style -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="6dp">

    <!-- Polaroid Frame (White Background) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/polaroid_background_rounded"
        android:padding="10dp"
        android:gravity="center_horizontal">

        <!-- Photo Container -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#F0F0F0">

            <!-- Photo -->
            <ImageView
                android:id="@+id/widget_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="#F0F0F0" />

            <!-- Default Heart when no photo -->
            <TextView
                android:id="@+id/widget_placeholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="💕"
                android:textSize="48sp"
                android:visibility="gone" />

        </FrameLayout>

        <!-- Missing You Text -->
        <TextView
            android:id="@+id/widget_missing_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Missing you"
            android:textColor="#333333"
            android:textSize="18sp"
            android:textStyle="italic"
            android:fontFamily="cursive"
            android:letterSpacing="0.05" />

    </LinearLayout>

    <!-- Hidden fields for compatibility -->
    <TextView
        android:id="@+id/widget_sender_name"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_timestamp"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_heart_icon"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/react_button"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone" />

</FrameLayout>
