<?xml version="1.0" encoding="utf-8"?>
<!-- Widget Style 1: Classic Photo Frame -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="@drawable/widget_background_classic">

    <!-- Partner Name with Heart -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">
        
        <TextView
            android:id="@+id/widget_partner_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Partner"
            android:textSize="16sp"
            android:textColor="#FF6B9D"
            android:textStyle="bold"
            android:drawableStart="@drawable/ic_heart_small"
            android:drawablePadding="4dp"/>
    </LinearLayout>

    <!-- Photo Container -->
    <FrameLayout
        android:id="@+id/photo_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_header"
        android:layout_above="@id/widget_footer"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp">

        <!-- Actual Photo -->
        <ImageView
            android:id="@+id/widget_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Partner photo"
            android:visibility="gone"/>

        <!-- Empty State -->
        <LinearLayout
            android:id="@+id/widget_placeholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/widget_empty_gradient">
            
            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:src="@drawable/ic_heart_outline"
                android:tint="#FF6B9D"
                android:alpha="0.6"/>
            
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No moments yet"
                android:textSize="14sp"
                android:textColor="#666666"
                android:layout_marginTop="8dp"/>
            
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Share your first moment!"
                android:textSize="12sp"
                android:textColor="#999999"
                android:layout_marginTop="4dp"/>
        </LinearLayout>
    </FrameLayout>

    <!-- Timestamp Footer -->
    <TextView
        android:id="@+id/widget_footer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Just now"
        android:textSize="12sp"
        android:textColor="#999999"
        android:drawableStart="@drawable/ic_time_small"
        android:drawablePadding="4dp"
        android:paddingTop="8dp"/>

</RelativeLayout>
