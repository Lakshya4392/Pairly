<?xml version="1.0" encoding="utf-8"?>
<!-- Widget Style 6: Flip Card - BACK SIDE (Note) -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/widget_background_flip">

    <!-- Card Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/flip_card_back_background"
        android:padding="20dp"
        android:gravity="center"
        android:elevation="4dp">

        <!-- Quote Icon Top -->
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_quote_left"
            android:tint="#9B59B6"
            android:alpha="0.4"
            android:layout_gravity="start"
            android:layout_marginBottom="12dp"/>

        <!-- Note Content -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="none">
            
            <TextView
                android:id="@+id/widget_note"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="I love you so much! ❤️\n\nYou make every day special."
                android:textSize="15sp"
                android:textColor="#333333"
                android:lineSpacingExtra="4dp"
                android:gravity="center"
                android:fontFamily="sans-serif-light"/>
        </ScrollView>

        <!-- Quote Icon Bottom -->
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_quote_right"
            android:tint="#9B59B6"
            android:alpha="0.4"
            android:layout_gravity="end"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Partner Signature -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_gravity="center">
            
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="— "
                android:textSize="14sp"
                android:textColor="#999999"/>
            
            <TextView
                android:id="@+id/widget_partner_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Partner"
                android:textSize="14sp"
                android:textColor="#9B59B6"
                android:textStyle="italic"/>
            
            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_heart_small"
                android:tint="#FF6B9D"
                android:layout_marginStart="6dp"/>
        </LinearLayout>

        <!-- Flip Back Indicator -->
        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_flip_back"
            android:tint="#9B59B6"
            android:alpha="0.6"
            android:layout_marginTop="16dp"/>
    </LinearLayout>

</RelativeLayout>
